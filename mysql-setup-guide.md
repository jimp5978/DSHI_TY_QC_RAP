# 회사 MySQL 연결 설정 가이드

## 📋 회사에서 확인해야 할 MySQL 정보

회사에 도착하면 다음 정보를 확인하고 Claude에게 요청하세요.

### 1. 필수 MySQL 연결 정보
- [ ] **호스트 주소**: `_______________` (예: `192.168.1.100` 또는 `db.company.com`)
- [ ] **포트**: `_______________` (기본값: `3306`)
- [ ] **사용자명**: `_______________` (예: `jimp5978` 또는 `dev_user`)
- [ ] **비밀번호**: `_______________`
- [ ] **데이터베이스명**: `_______________` (접근할 DB 이름)

### 2. 네트워크 확인사항
- [ ] 외부에서 MySQL 서버 접근 가능한지 확인
- [ ] 방화벽 설정 확인 (포트 3306 열려있는지)
- [ ] VPN 연결 필요 여부 확인
- [ ] SSL 연결 사용 여부 확인

## 🤖 Claude에게 요청할 명령어

### MySQL 연결 정보 업데이트
정보를 확인했으면 아래 문구를 Claude에게 복사해서 요청하세요:

```
MySQL MCP 서버의 연결 정보를 다음과 같이 업데이트해주세요:

- 호스트: [실제 호스트 주소]
- 포트: [실제 포트 번호]  
- 사용자명: [실제 사용자명]
- 비밀번호: [실제 비밀번호]
- 데이터베이스: [실제 DB명]

설정 완료 후 연결 테스트도 진행해주세요.
```

### 연결 테스트 요청
```
MySQL MCP 서버가 정상적으로 연결되는지 테스트해주세요. 
디버그 모드로 실행하여 연결 상태를 확인하고, 
기본적인 데이터베이스 정보 조회도 시도해보세요.
```

### 데이터베이스 작업 예시
```
현재 연결된 MySQL 데이터베이스에서 다음 작업을 해주세요:
1. 전체 테이블 목록 조회
2. [특정 테이블명] 테이블의 구조 확인
3. [특정 테이블명]에서 최근 10개 레코드 조회
```

## ⚠️ 보안 및 주의사항

### 보안 고려사항
- 가능하면 **읽기 전용 계정** 사용
- 비밀번호는 **강력한 패스워드** 사용
- **SSL 연결** 활성화 권장
- 회사 **보안 정책** 확인 및 준수

### 백업 명령어 (문제 발생 시)
```bash
# 현재 MCP MySQL 설정 백업
claude mcp list > mcp-backup.txt

# MySQL MCP 제거 (문제 발생 시)
claude mcp remove mcp_mysql

# MySQL MCP 재설치
claude mcp add --scope user mcp_mysql npx @benborla29/mcp-server-mysql -e MYSQL_HOST=[호스트] -e MYSQL_USER=[사용자명] -e MYSQL_PASSWORD=[비밀번호] -e MYSQL_DATABASE=[DB명]
```

## 📝 추가 참고사항

### MySQL 서버 정보 확인 방법
회사에서 MySQL 정보를 모르는 경우, 다음 방법으로 확인:

1. **시스템 관리자에게 문의**
2. **기존 애플리케이션 설정 파일 확인**
3. **phpMyAdmin 등 관리 도구 확인**
4. **MySQL Workbench 연결 정보 확인**

### 연결 문제 해결
- 방화벽 차단: IT 팀에 포트 오픈 요청
- 인증 실패: 사용자명/비밀번호 재확인
- 호스트 연결 실패: IP 주소/도메인 재확인
- SSL 오류: SSL 설정 확인

---

## 🚀 빠른 시작 체크리스트

회사 도착 후:
1. [ ] MySQL 서버 정보 수집
2. [ ] 네트워크 접근 가능 여부 확인  
3. [ ] Claude에게 설정 업데이트 요청
4. [ ] 연결 테스트 실행
5. [ ] 기본 데이터베이스 작업 테스트

**성공하면 집에서도 회사 DB에 원격 접근 가능! 🎉**

---
*생성일: 2025-07-20*  
*작성자: Claude Code Assistant*